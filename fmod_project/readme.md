This folder is for the FMOD Studio project (optional)
All its contents are ignored by default.

You can provide the full path of the FMOD Studio project on your IDE to debug your plugins.\
Finally, in FMOD Studio, you can define the location of your plugins by setting a base folder in "Edit" > "Preferences" > "Assets" > "Plug-ins Folder".
